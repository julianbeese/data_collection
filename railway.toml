[build]
builder = "NIXPACKS"

[deploy]
startCommand = "/opt/venv/bin/python run_streamlit.py"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Explizite Konfiguration für Railway
[railway]
startCommand = "/opt/venv/bin/python run_streamlit.py"
